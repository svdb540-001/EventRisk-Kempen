{
  "name": "eventrisk-kempen",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @eventrisk/web run dev",
    "dev:api": "npm --workspace @eventrisk/api run start:dev",
    "db:generate": "npm --workspace @eventrisk/api run db:generate",
    "db:push": "npm --workspace @eventrisk/api run db:push",
    "db:studio": "npm --workspace @eventrisk/api run db:studio"
  }
}
