# EventRisk Kempen

Monorepo met:
- `apps/web` (Next.js)
- `apps/api` (NestJS)

## Development

```bash
npm install
npm run dev:web
npm run dev:api
```

## Omgevingsvariabelen

Kopieer `.env.example` naar `.env` in de repo root.
